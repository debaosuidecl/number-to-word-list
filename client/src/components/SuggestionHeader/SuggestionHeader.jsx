import classes from "./SuggestionHeader.module.css"

const SuggestionHeader = (props) => {
    return <div className={classes.SuggestionHeader}>
        <h1>Suggestion List</h1>
    </div>
}

export default SuggestionHeader